# プロジェクト「URL Capture」の問題点分析

このドキュメントは、VSCode拡張機能「URL Capture」のコードベースを分析し、特定された問題点をまとめたものです。問題は重要度に応じて分類されています。

## 1. 重大な問題 (Critical Issues)

アプリケーションの安定性や基本機能に深刻な影響を与える、または早急な対応が求められる問題です。

### 1.1. 自動テストの欠如
- **問題点:** プロジェクトには自動テストが全く存在しません。`src/test/extension.test.ts` はVSCodeのデフォルトテンプレートのままで、機能のテストが一切実装されていません。
- **影響:**
    - バグの早期発見が困難です。
    - リファクタリングや機能追加を行う際に、既存の機能を破壊してしまう（デグレード）リスクが非常に高いです。
    - コードの品質と信頼性を保証できません。
- **提案:** Mochaを使用した単体テストと統合テストを導入し、主要な機能（URLのフォーマット、ファイル名の生成、エラーハンドリング、Puppeteerの動作など）をカバーするテストケースを作成すべきです。

### 1.2. 不適切なスクリーンショットのビューポート（表示領域）計算
- **問題点:** `src/extension.ts` の `captureWebsite` 関数内にあるスクリーンショットの幅を計算するロジック (`Math.round((document.body.scrollWidth + 1920) / 2)`) が不適切です。ページのスクロール幅に固定値 `1920` を足して2で割るという計算には論理的な根拠がなく、意図しない幅のスクリーンショットが生成される可能性があります。
- **影響:** ユーザーが期待するウェブサイトの表示と異なる、不完全または歪んだ画像が保存されてしまいます。
- **提案:** `document.body.scrollWidth` をそのまま使用するか、もしくはユーザーが設定可能な固定の幅（例: 1920px）をデフォルト値として使用するべきです。

### 1.3. 不十分なエラーハンドリング
- **問題点:** `getActiveWorkspaceFolder` 関数は、ワークスペースが開かれていない場合にエラーメッセージを表示しますが、`undefined` を返します。コマンドの実行は継続され、`undefined` がファイルパスの一部として使われるため、`captureWebsite` 関数内で分かりにくいエラーが発生します。
- **影響:** ユーザーは「ワークスペースが開かれていない」という根本原因に気づけず、問題の解決が困難になります。
- **提案:** `getActiveWorkspaceFolder` で `undefined` が返された場合、コマンドの実行をその場で停止し、ユーザーに明確なエラーメッセージ（例: 「画像を保存するフォルダが見つかりません。ワークスペースを開いてください。」）を提示すべきです。

## 2. 主要な問題 (Major Issues)

UX（ユーザー体験）やパフォーマンス、アーキテクチャに大きな影響を与える問題です。

### 2.1. パフォーマンスの非効率性
- **問題点:** URLをキャプチャするたびに `puppeteer.launch()` を呼び出し、新しいブラウザインスタンスを起動しています。ブラウザの起動はリソースを大量に消費する重い処理です。
- **影響:** コマンド実行のたびに遅延が発生し、特に連続して使用する場合のUXが著しく低下します。また、システムのメモリやCPUに不要な負荷をかけます。
- **提案:** 拡張機能のアクティベート時、または最初のキャプチャ時に一度だけブラウザインスタンスを起動し、以降はそのインスタンスを再利用するべきです。拡張機能が非アクティブ化される際にインスタンスを閉じる処理を追加します。

### 2.2. 直感的でないファイルの保存場所
- **問題点:** スクリーンショット画像は、現在アクティブなファイルがあるディレクトリではなく、ワークスペースのルートフォルダに保存されます。
- **影響:** ユーザーは関連ファイルを近くに置きたいと考えるのが一般的であり、現在の仕様はユーザーの期待と異なり、不便です。
- **提案:** `vscode.window.activeTextEditor.document.uri` からファイルのパスを取得し、`path.dirname()` を使ってそのファイルが存在するディレクトリに画像を保存するように変更すべきです。

### 2.3. 依存関係の肥大化
- **問題点:** `puppeteer` は非常に巨大なライブラリであり、内部で完全なChromiumブラウザをダウンロードします。これにより、拡張機能のインストールサイズが数百MB単位で増加します。
- **影響:** ユーザーのディスク容量を大きく圧迫し、インストールやアップデートに時間がかかります。
- **提案:**
    - **短期:** `puppeteer-core` を使用し、ユーザーがローカルにインストール済みのChromeブラウザを利用するよう促す方式に変更する。これにより、拡張機能自体のサイズを劇的に削減できます。
    - **長期:** より軽量なスクリーンショット取得方法（例: 外部APIサービスの利用、もしくは別の軽量ライブラリの検討）を模索することも考えられます。

## 3. 軽微な問題と改善案 (Minor Issues & Improvements)

修正が望ましい比較的小さな問題や、拡張機能の利便性を向上させるための提案です。

- **設定機能の欠如:** ファイル名の形式、保存先のカスタマイズ、画像の品質、タイムアウト時間などをユーザーが `settings.json` で設定できる機能がありません。
- **ハードコードされたタイムアウト:** `page.goto` のタイムアウトが2.5秒に固定されています。低速なネットワーク環境や重いウェブサイトでは短すぎるため、設定可能にすることが望ましいです。
- **モジュールインポートの不統一:** `import` と `require` が混在しています (`import * as puppeteer from 'puppeteer';` に統一すべきです)。
- **ドキュメントの誤字:** `package.json` の `description` に含まれる日本語「画損」は「画像」の誤りです。
- **ユーザーフィードバックの不足:** テキストが選択されていない場合、コマンドは何もせずに終了しますが、「キャプチャ対象のURLが選択されていません」といったメッセージをユーザーに提示する方が親切です。
- **`activationEvents` の明示:** `package.json` の `activationEvents` が空です。`onCommand:url-capture.url-capture` を明示的に指定することが推奨されます。
- **不要なグローバル宣言:** `declare const document: any;` というグローバルスコープでの宣言は、コードの意図を不明確にする可能性があるため、より適切なスコープに配置するか、`page.evaluate` の型定義を工夫することで回避すべきです。
